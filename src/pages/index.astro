---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Driftless Logic LLC">
  <header>
    <nav>
      <div class="logo">Driftless Logic</div>
      <ul>
        <li><a href="#services">Services</a></li>
        <li><a href="#about">About</a></li>
        <li><a href="#contact">Contact</a></li>
      </ul>
    </nav>
  </header>

  <main>
    <section class="hero">
      <h1>AI Solutions for <span class="highlight">Complex Problems</span></h1>
      <p>We build intelligent systems that transform how businesses operate.</p>
      <a href="#contact" class="cta">Get in Touch</a>
    </section>

    <section id="services" class="services">
      <h2>What We Do</h2>
      <div class="service-grid">
        <div class="service-card">
          <h3>AI Strategy</h3>
          <p>Navigate the AI landscape with clear, actionable strategies tailored to your business goals.</p>
        </div>
        <div class="service-card">
          <h3>Custom Development</h3>
          <p>Purpose-built AI solutions designed to solve your specific challenges.</p>
        </div>
        <div class="service-card">
          <h3>Integration</h3>
          <p>Seamlessly connect AI capabilities with your existing systems and workflows.</p>
        </div>
      </div>
    </section>

    <section id="about" class="about">
      <h2>About Driftless Logic</h2>
      <p>We're a team dedicated to making AI practical and accessible for businesses of all sizes. Based in the Driftless region, we bring a grounded, thoughtful approach to solving complex problems.</p>
    </section>

    <section id="contact" class="contact">
      <h2>Let's Talk</h2>
      <p>Ready to explore what AI can do for your business?</p>
      <a href="mailto:hello@driftlesslogic.com" class="cta">Contact Us</a>
    </section>
  </main>

  <footer>
    <p>&copy; {new Date().getFullYear()} Driftless Logic LLC. All rights reserved.</p>
  </footer>
</Layout>

<style>
  header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    background: rgba(10, 10, 15, 0.9);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  nav {
    max-width: 1200px;
    margin: 0 auto;
    padding: 1rem 2rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .logo {
    font-size: 1.25rem;
    font-weight: 600;
    letter-spacing: -0.02em;
  }

  nav ul {
    display: flex;
    list-style: none;
    gap: 2rem;
  }

  nav a {
    color: var(--color-text-muted);
    font-size: 0.9rem;
  }

  nav a:hover {
    color: var(--color-text);
  }

  main {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  section {
    padding: 6rem 0;
  }

  .hero {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding-top: 4rem;
  }

  .hero h1 {
    font-size: clamp(2.5rem, 6vw, 4rem);
    font-weight: 700;
    line-height: 1.1;
    letter-spacing: -0.03em;
    margin-bottom: 1.5rem;
  }

  .highlight {
    color: var(--color-accent);
  }

  .hero p {
    font-size: 1.25rem;
    color: var(--color-text-muted);
    max-width: 600px;
    margin-bottom: 2rem;
  }

  .cta {
    display: inline-block;
    background: var(--color-accent);
    color: white;
    padding: 0.875rem 2rem;
    border-radius: 8px;
    font-weight: 500;
    transition: background 0.2s ease, transform 0.2s ease;
  }

  .cta:hover {
    background: var(--color-accent-hover);
    color: white;
    transform: translateY(-2px);
  }

  h2 {
    font-size: 2rem;
    margin-bottom: 2rem;
    letter-spacing: -0.02em;
  }

  .service-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1.5rem;
  }

  .service-card {
    background: var(--color-bg-secondary);
    padding: 2rem;
    border-radius: 12px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    transition: border-color 0.2s ease;
  }

  .service-card:hover {
    border-color: var(--color-accent);
  }

  .service-card h3 {
    font-size: 1.25rem;
    margin-bottom: 0.75rem;
  }

  .service-card p {
    color: var(--color-text-muted);
  }

  .about p,
  .contact p {
    color: var(--color-text-muted);
    max-width: 600px;
    margin-bottom: 1.5rem;
  }

  footer {
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    padding: 2rem;
    text-align: center;
    color: var(--color-text-muted);
    font-size: 0.875rem;
  }

  @media (max-width: 768px) {
    nav {
      flex-direction: column;
      gap: 1rem;
    }

    nav ul {
      gap: 1.5rem;
    }

    section {
      padding: 4rem 0;
    }
  }
</style>
