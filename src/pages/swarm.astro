---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<Layout title="Driftless Swarm - AI Agent Orchestration Platform">
  <Header />

  <!-- Hero Section -->
  <section class="relative pt-20 pb-20 overflow-hidden bg-white">
    <div class="absolute inset-0 bg-topo-pattern pointer-events-none opacity-100"></div>
    <div class="absolute top-0 right-0 w-[500px] h-[500px] bg-primary/10 rounded-full blur-[100px] pointer-events-none mix-blend-multiply"></div>
    <div class="absolute bottom-0 left-0 w-[400px] h-[400px] bg-accent-orange/10 rounded-full blur-[100px] pointer-events-none mix-blend-multiply"></div>

    <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <div class="inline-flex items-center gap-2 rounded-full border border-primary/30 bg-primary/10 px-3 py-1 mb-8 shadow-sm">
        <span class="material-symbols-outlined text-primary text-sm">hive</span>
        <span class="text-xs font-medium text-green-700">Coming Soon</span>
      </div>

      <h1 class="max-w-4xl mx-auto text-4xl md:text-6xl font-bold tracking-tighter text-gray-900 mb-6 leading-[1.1]">
        Your competitors are building with agents. <span class="text-primary">Are you?</span>
      </h1>

      <p class="max-w-2xl mx-auto text-lg text-gray-500 leading-relaxed">
        Driftless Swarm is an AI agent orchestration platform designed for enterprise software development. Coming soon.
      </p>
    </div>
  </section>

  <!-- Provocations -->
  <section class="py-20 bg-gray-50 border-y border-gray-200">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-8 max-w-4xl mx-auto">
        <div class="bg-white rounded-xl p-8 border border-gray-200 shadow-sm">
          <span class="material-symbols-outlined text-primary text-3xl mb-4">help</span>
          <p class="text-xl font-semibold text-gray-900">Your customers are asking about AI. What's your answer?</p>
        </div>
        <div class="bg-white rounded-xl p-8 border border-gray-200 shadow-sm">
          <span class="material-symbols-outlined text-primary text-3xl mb-4">speed</span>
          <p class="text-xl font-semibold text-gray-900">Still sizing stories while agents ship features?</p>
        </div>
        <div class="bg-white rounded-xl p-8 border border-gray-200 shadow-sm">
          <span class="material-symbols-outlined text-primary text-3xl mb-4">timer</span>
          <p class="text-xl font-semibold text-gray-900">How many sprints until you realize the sprint is the problem?</p>
        </div>
        <div class="bg-white rounded-xl p-8 border border-gray-200 shadow-sm">
          <span class="material-symbols-outlined text-primary text-3xl mb-4">rocket_launch</span>
          <p class="text-xl font-semibold text-gray-900">What would your team build if coordination cost dropped to zero?</p>
        </div>
      </div>
    </div>
  </section>

  <!-- The Promise -->
  <section class="py-24 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="max-w-3xl mx-auto text-center mb-16">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-6">The Promise</h2>
        <p class="text-lg text-gray-500">
          Driftless Swarm puts AI agents to work as first-class members of your development team. <strong class="text-gray-900">Not copilots. Not assistants. Workers</strong>—guided by humans, executing autonomously, delivering continuously.
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 max-w-4xl mx-auto">
        <div class="text-center p-6">
          <div class="size-16 rounded-full bg-primary/10 flex items-center justify-center text-primary mx-auto mb-4">
            <span class="material-symbols-outlined text-3xl">account_tree</span>
          </div>
          <p class="font-semibold text-gray-900">Built on the Driftless methodology</p>
        </div>
        <div class="text-center p-6">
          <div class="size-16 rounded-full bg-primary/10 flex items-center justify-center text-primary mx-auto mb-4">
            <span class="material-symbols-outlined text-3xl">shield</span>
          </div>
          <p class="font-semibold text-gray-900">Designed for federal compliance</p>
        </div>
        <div class="text-center p-6">
          <div class="size-16 rounded-full bg-primary/10 flex items-center justify-center text-primary mx-auto mb-4">
            <span class="material-symbols-outlined text-3xl">trending_up</span>
          </div>
          <p class="font-semibold text-gray-900">Ready for enterprise scale</p>
        </div>
      </div>
    </div>
  </section>

  <!-- What's Coming -->
  <section class="py-24 bg-background-alt relative overflow-hidden">
    <div class="absolute inset-0 bg-topo-pattern opacity-40 pointer-events-none"></div>
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
      <div class="max-w-3xl mx-auto text-center mb-16">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">What's Coming</h2>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 max-w-4xl mx-auto">
        <div class="glass-card p-8 rounded-xl text-center">
          <div class="size-12 rounded-lg bg-gray-50 border border-gray-200 flex items-center justify-center mx-auto mb-6 text-gray-700">
            <span class="material-symbols-outlined">hub</span>
          </div>
          <h3 class="text-lg font-bold text-gray-900 mb-2">Multi-agent Task Orchestration</h3>
          <p class="text-gray-500 text-sm">Coordinate multiple AI agents working in parallel on complex objectives.</p>
        </div>

        <div class="glass-card p-8 rounded-xl text-center">
          <div class="size-12 rounded-lg bg-gray-50 border border-gray-200 flex items-center justify-center mx-auto mb-6 text-gray-700">
            <span class="material-symbols-outlined">supervisor_account</span>
          </div>
          <h3 class="text-lg font-bold text-gray-900 mb-2">Human-in-the-loop Oversight</h3>
          <p class="text-gray-500 text-sm">Guide and supervise agent work with intuitive oversight controls.</p>
        </div>

        <div class="glass-card p-8 rounded-xl text-center">
          <div class="size-12 rounded-lg bg-gray-50 border border-gray-200 flex items-center justify-center mx-auto mb-6 text-gray-700">
            <span class="material-symbols-outlined">sync</span>
          </div>
          <h3 class="text-lg font-bold text-gray-900 mb-2">Continuous Delivery</h3>
          <p class="text-gray-500 text-sm">Ship software continuously without artificial sprint boundaries.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Waitlist Section -->
  <section class="py-24 bg-white border-t border-gray-200">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="max-w-2xl mx-auto text-center">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">Join the Waitlist</h2>
        <p class="text-gray-500 mb-8">
          Driftless Swarm is currently in private development. Join the waitlist for early access, partnership opportunities, or just to stay informed.
        </p>

        <form class="space-y-4 text-left" action="https://formspree.io/f/your-form-id" method="POST">
          <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
            <div>
              <label for="name" class="block text-sm font-medium text-gray-700 mb-1">Name</label>
              <input type="text" id="name" name="name" required class="w-full rounded-lg border border-gray-300 px-4 py-3 text-gray-900 focus:border-primary focus:ring-primary" />
            </div>
            <div>
              <label for="email" class="block text-sm font-medium text-gray-700 mb-1">Email</label>
              <input type="email" id="email" name="email" required class="w-full rounded-lg border border-gray-300 px-4 py-3 text-gray-900 focus:border-primary focus:ring-primary" />
            </div>
          </div>
          <div>
            <label for="organization" class="block text-sm font-medium text-gray-700 mb-1">Organization</label>
            <input type="text" id="organization" name="organization" class="w-full rounded-lg border border-gray-300 px-4 py-3 text-gray-900 focus:border-primary focus:ring-primary" />
          </div>
          <div>
            <label for="interest" class="block text-sm font-medium text-gray-700 mb-1">Interest</label>
            <select id="interest" name="interest" class="w-full rounded-lg border border-gray-300 px-4 py-3 text-gray-900 focus:border-primary focus:ring-primary">
              <option value="early-access">Early Access</option>
              <option value="partnership">Partnership</option>
              <option value="licensing">Licensing</option>
              <option value="curious">Just Curious</option>
            </select>
          </div>
          <button type="submit" class="w-full flex items-center justify-center gap-2 rounded-lg bg-primary px-8 py-4 text-base font-bold text-black hover:bg-green-400 transition-all shadow-lg shadow-primary/30">
            Join the Waitlist
            <span class="material-symbols-outlined text-[20px]">arrow_forward</span>
          </button>
        </form>

        <p class="text-center text-gray-900 font-semibold mt-12 text-lg">
          The way software gets built is about to change. Get there first.
        </p>
      </div>
    </div>
  </section>

  <Footer />
</Layout>
